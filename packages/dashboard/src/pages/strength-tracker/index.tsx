import { Typography } from 'antd';

const { Title, Text } = Typography;

export default function StrengthTrackerPage() {
  return (
    <div style={{ padding: 24 }}>
      <Title level={2}>Strength Tracker</Title>
      <Text type="secondary">Coming in Group 4 — data connection via Eden Treaty.</Text>
    </div>
  );
}
